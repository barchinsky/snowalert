package com.m.snowalert.dashboard.usecase

import javax.inject.Inject

class FetchForecastInfoUseCase @Inject constructor() {

    suspend fun execute() {
        // Fetch forecast info from remote and store to repo
    }
}